import type { ReactNode } from "react";

export const metadata = {
  title: "Services & Pricing",
  description: "Website design and development services: business websites, landing pages, e-commerce, redesigns and maintenance.",
};

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
