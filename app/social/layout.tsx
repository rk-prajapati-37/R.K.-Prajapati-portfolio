import type { ReactNode } from "react";

export const metadata = {
  title: "Social Media",
  description: "Follow R.K. Prajapati on YouTube, Facebook, LinkedIn and Instagram for web design updates.",
};

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
